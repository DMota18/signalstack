services:
  # --- Caddy (reverse proxy + automatic HTTPS via Let's Encrypt) ---
  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./frontend/dist:/srv/frontend:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

  # --- FastAPI Backend ---
  api:
    build: .
    expose:
      - "8000"
    env_file: .env
    environment:
      - APP_ENV=production
      - DEBUG=false
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      # raise_for_status() matters: httpx.get does not raise on 4xx/5xx,
      # so without it a 500ing API still reports healthy
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health').raise_for_status()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 1024M

  # --- Celery Worker ---
  celery-worker:
    build: .
    command: celery -A backend.jobs.celery_app worker -Q intelligence,sync,monitor,maintenance --loglevel=info --concurrency=2
    env_file: .env
    environment:
      - APP_ENV=production
      - DEBUG=false
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

  # --- Celery Beat ---
  celery-beat:
    build: .
    command: celery -A backend.jobs.celery_app beat --loglevel=info
    env_file: .env
    environment:
      - APP_ENV=production
      - DEBUG=false
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  # --- Redis ---
  redis:
    image: redis:7-alpine
    # ${VAR:?...} makes compose refuse to start without a real password —
    # the old ':-changeme' default deployed quietly insecure
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env} --maxmemory 128mb --maxmemory-policy allkeys-lru
    expose:
      - "6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

volumes:
  redis-data:
  caddy-data:
  caddy-config:
